# Runtime defaults for standalone containers. Home Assistant uses
# doorbell/config.yaml and writes the selected options to /data/options.json.
doorbells: []
system:
  log_level: INFO
  sdk_log_level: NONE
  health_check_interval: 30
mqtt: null
